---
description: Workspace rules for monolith_demo — where to start, what is authoritative, and the md/html convention. Mirrors CLAUDE.md / AGENTS.md.
globs:
alwaysApply: true
---

# Workspace rules — monolith_demo

Follow the reading order below.

## Where to start
- **Knowledge base / index:** `apps/MANIFEST.json` — machine-readable navigation of the repo.
- **Knowledge base:** `apps/` — the indexed knowledge base.

## Conventions
- `.md` files are the AI-optimized source of truth; `.html` files are the human view, generated from the `.md`.
- Every referenced path must resolve. Keep CLAUDE.md and AGENTS.md byte-identical.

<!-- Generated by the Workspace Optimizer; mirrors CLAUDE.md/AGENTS.md. -->
