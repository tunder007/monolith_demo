# monolith_demo

monolith_demo — see the knowledge base for the full description.

## Where to start (any agent)

- **Knowledge base / index:** `apps/MANIFEST.json` — machine-readable navigation of the repo.
- **Knowledge base:** `apps/` — the indexed knowledge base.

## Top-level structure

- `apps/` — the indexed knowledge base.
- `docs/` — the indexed knowledge base.
- `packages/` — the indexed knowledge base.
- `templates/` — the indexed knowledge base.

## Benchmarking & quality gate (any agent)

Score this repo on **AI-optimization** and **determinism** before/after substantial changes with the Deterministic Checker (if present):

    node tasks/skill-deterministic-checker/scripts/check.mjs . --out /tmp/bench

## Conventions

- **`.md` files are the AI-optimized source of truth; `.html` files are the human view, generated from the `.md`.**
- Predictable naming/numbering; a uniform schema per artifact type; every referenced path resolves.

<!-- Generated by the Workspace Optimizer. Edit the structure/config and re-run; this file is regenerated. -->
