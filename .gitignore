# Environment files
**/.env
**/.env.local
**/.env.development.local
**/.env.test.local
**/.env.production.local
!**/.env.example

# Dependencies
**/node_modules

# Build outputs
**/dist
**/.next
**/out
**/build

# Monorepo tooling
.turbo
**/.turbo
.pnpm-store

# npm-first: package-lock.json is the committed lockfile. pnpm still works
# locally (both PMs supported) but its lockfile is not committed.
pnpm-lock.yaml
**/pnpm-lock.yaml

# CLI build artifact: templates are bundled into the package at build time
/apps/cli/templates

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# OS / editor noise
.DS_Store
.idea
.vscode

